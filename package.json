{
  "name": "que",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @que/web dev",
    "build": "pnpm --filter @que/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@11.9.0",
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "pg": "^8.22.0"
  }
}

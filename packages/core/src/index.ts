// @que/core — 도메인 타입, 검증 스키마, 권한 규칙, 데이터 접근 계층.
// 웹(apps/web), MCP 서버, CLI가 모두 이 패키지를 통해서만 데이터에 접근한다.
// 데이터 모델은 Phase 2에서 docs/que-product-plan.md의 "데이터 모델 초안" 기준으로 구현한다.

export const CORE_VERSION = "0.1.0";

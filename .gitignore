.DS_Store
node_modules/
.next/
.claude/settings.local.json
.playwright-mcp/
*.png
!apps/web/public/**

# 비밀값 — 절대 커밋 금지 (Supabase secret key 등)
.env
.env.*
!.env.example
data/.env
data/.env.*
.mcp.json

# 구 스키마 백업(민감 데이터) — 커밋 금지
db/supabase/backup-before-que/
.vercel
.env*.local

# PAT 평문(팀원 개별 전달용) — 커밋 금지
data/pat-tokens.txt

# 개인 초기 비밀번호 평문 + 적용 SQL(해시) — 커밋 금지
data/passwords.txt
db/supabase/set-passwords.sql

# 유저 PII(전화/생년월일) — 커밋 금지
data/docs/que-user-info.md
